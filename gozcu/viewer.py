"""Streamlit arayüzü — sorgu kutusu → küçük resim ızgarası → tıkla, videoyu o andan izle.

Çalıştırma: streamlit run gozcu/viewer.py
"""

# ── Sayfa düzeni: sorgu kutusu + top-k seçici ──
# ── Sonuç ızgarası: thumb + zaman damgası + skor ──
# ── Tıklanınca: st.video(video_path, start_time=ts_offset) ──

if __name__ == "__main__":
    raise NotImplementedError("Faz 1 — sıradaki implementasyon adımı")
